export enum EventTrackerAction {
  CLICK = "click",
  HOVER = "hover",
  SEEN = "seen",
}

export enum EventAction {
  CLICK = "click",
  HOVER = "hover",
}

export enum NativeEvent {
  "click" = "click",
  "hover" = "mouseover",
}
